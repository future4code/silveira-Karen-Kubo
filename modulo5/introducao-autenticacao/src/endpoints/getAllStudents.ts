import {Request, Response} from 'express';
import { StudentDatabase } from '../data/StudentDatabase';

export default async function (req:Response, res:Response):Promise<void> {
    try {
        
    } catch (error:any) {
        
    }
}